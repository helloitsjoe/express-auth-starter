steps:
  - label: 'Testing :musical_note:'
    command: echo "Hello!"

  - label: 'Running tests'
    command: yarn && yarn test
    # How to use docker?
    # plugins:
    #   - docker#v3.5.0:
    #       image: 'node:12-alpine'
