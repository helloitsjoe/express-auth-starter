steps:
  # - label: 'Testing :musical_note:'
  #   command: echo "Hello!"

  # TODO: Figure out how to pass artifacts across steps
  # - label: 'Install'
  #   command: yarn
  # artifact_paths:
  #   - 'node_modules/**/*'

  - label: 'Jest tests'
    command: yarn && yarn test
  # depends_on: 'install'
  # How to use docker?

  # - wait

  - label: 'Cypress Tests'
    command: 'yarn && yarn cy:run:ci'
    # plugins:
    # - docker-compose#v3.3.0
    # run: auth
    # command: yarn && yarn cy:run
  # depends_on: 'install'
